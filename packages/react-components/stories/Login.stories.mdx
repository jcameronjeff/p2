import { Meta, Story, Canvas } from '@storybook/addon-docs';
import { Button } from '../lib';

<Meta title="Example/Views/LoginDialog" component={Button} />

# Button

With `MDX` we can define a story for `Button` right in the middle of our
markdown documentation.

<Canvas>
<Story name="All Buttons">
<form className='card w-[400px]'>
  <header className='flex items-center justify-center'>
    <img className='h-[48px] w-[48px]' src='https://api.manheim.com/assets/images/manheimLogo.svg' />
  </header>
  <main className='space-y-4'>
    <h1 className='text-center'>Sign In</h1>
    <div className='space-y-2'>
      <input type='text' className='w-full' placeholder="Username" />
      <input type='password' className='w-full' placeholder="Password" />
    </div>
    <div>
      <input type='checkbox' checked />
      <label>Remember my username</label>
    </div>
    <button type='submit' className='w-full center'>Login</button>
  </main>
  <footer className='flex justify-between items-center'>
    <div>Forgot?</div>
    <div className='text-xs'>
      <button className='aux p-2'>Username?</button>
      <button className='aux p-2'>Password?</button>
    </div>
  </footer>
</form>
</Story>
</Canvas>

# Usage

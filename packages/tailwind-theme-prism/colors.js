const colors = require('./colors.json')

module.exports = {
  ...colors,
  cerulean: colors.blue,
  sky: colors.blue,
  navy: colors.blue,
  amber: colors.orange,
  saffron: colors.orange,
  gold: colors.yellow,
  meadow: colors.green,
  jungle: colors.emerald,
  ocean: colors.teal
};

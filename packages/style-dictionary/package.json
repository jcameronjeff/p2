{
  "name": "@prism2/style-dictionary",
  "version": "2.0.9-alpha.6",
  "description": "(WIP): Canonical repository of platform-agnostic design tokens and build tools",
  "scripts": {
    "prepare": "npm run build",
    "build": "node build.js",
    "clean": "rm -rf build",
    "watch": "npm run build && chokidar  \"tokens/**/*.json\" \"config.json\" \"config-full.json\" \"build.js\" -c \"npm run build\""
  },
  "files": [
    "build"
  ],
  "dependencies": {
    "@divriots/style-dictionary-to-figma": "^0.4.0",
    "style-dictionary": "^3.7.2",
    "style-dictionary-utils": "^1.1.0"
  },
  "devDependencies": {
    "chokidar-cli": "^3.0.0",
    "color2k": "^2.0.2",
    "sd-tailwindcss-transformer": "^1.2.0"
  }
}
